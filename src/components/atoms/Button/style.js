import styled from 'styled-components';
import { COLOR } from '../../../styles/colors';

const StyledButton = styled.button`
  width: 100%;
  height: 30px;
  border-radius: 3px;
  font-weight: 700;
  color: white;
  background-color: ${COLOR.BLUE_0};
`;

export { StyledButton };

import styled from 'styled-components';
import { COLOR } from '../../../styles/colors';

const MainLayout = styled.div`
  width: 100%;
`;

const MainContainer = styled.div`
  width: 100%;
  height: 100%;
  padding: 18px 0;
  background-color: ${COLOR.GRAY_0};
`;

export { MainLayout, MainContainer };
